[model]
name = Date
label = {{ this.datetime.strftime('%Y-%m-%d') }}
hidden = yes

[fields.date]
label = Date
type = datetime

[fields.date_end]
label = Date
type = datetime
description = The end of the day, This is a hack because is not possible to compare onlly the date in filter expresions
